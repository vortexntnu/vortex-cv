# RoboSub launch files

Launch files for the RoboSub mission go here (the behavior tree runner and
the task-specific perception). The trees are in
`behavior_trees/robosub/`, their values in `config/mission/robosub/`.

TACC missions (pipeline inspection, subsea docking, valve intervention) are
in `launch/mission/tacc/`.
